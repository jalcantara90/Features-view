// Common
import React from 'react'

// Styled Components
import { Container } from './styles'

export const FeaturesContent = ({ children }) => {
  return (
    <Container>
      {children}
    </Container>
  )
}
